@extends ('layouts.app')

@section ('content')
@if ($errors->any())
          <div class="alert alert-danger">
              <ul>
                  @foreach ($errors->all() as $error)
                      <li>{{ $error }}</li>
                  @endforeach
              </ul>
          </div><br />
      @endif

{{ Form::model($coral, array('route' => array('corals.update', $coral->id), 'method' => 'PUT')) }}

    <div class="form-group">
        {{ Form::label('item_number', 'Item Number') }}
        {{ Form::text('item_number', null) }}
    </div>

    <div class="form-group">
        {{ Form::label('name', 'Name') }}
        {{ Form::text('name', null) }}
    </div>
    <div>
	<img src="uploads/photo/">
    </div>
    <div class="form-group">
	{{ Form::label('photo','Upload image' )}}
	{{ Form::file('photo', null) }}
    </div>
    <div class="form-group">
        {{ Form::label('plastic_quantity', 'Plastic Quantity') }}
        {{ Form::text('plastic_quantity', null) }}
    </div>
    <div class="form-group">
        {{ Form::label('cost_price', 'Cost Price') }}
        {{ Form::text('cost_price', null) }}
    </div>
    <div class="form-group">
        {{ Form::label('product_weight', 'Product Weight') }}
        {{ Form::text('product_weight', null) }}
    </div>
    <div class="form-group">
        {{ Form::label('retail_price', 'Retail Price') }}
        {{ Form::text('retail_price', null) }}
    </div>
    <div class="form-group">
        {{ Form::label('wholesale_price', 'Wholesale Price') }}
        {{ Form::text('wholesale_price', null) }}
    </div>
    <div class="form-group">
        {{ Form::label('barcode', 'Barcode') }}
        {{ Form::text('barcode', null) }}
    </div>
    <div class="form-group">
        {{ Form::label('description', 'Description') }}
        {{ Form::text('description', null) }}
    </div>


    {{ Form::submit('Edit the Coral!', array('class' => 'btn btn-primary')) }}

@endsection